
. ~/setupfiles/zsh/prompt
. ~/setupfiles/zsh/aliases
. ~/setupfiles/zsh/git
. ~/setupfiles/zsh/hue
. ~/setupfiles/zsh/keys

#load colors
autoload colors && colors
for COLOR in RED GREEN YELLOW BLUE MAGENTA CYAN BLACK WHITE; do
    eval $COLOR='%{$fg_no_bold[${(L)COLOR}]%}'  #wrap colours between %{ %} to avoid weird gaps in autocomplete
    eval BOLD_$COLOR='%{$fg_bold[${(L)COLOR}]%}'
done
eval RESET='$reset_color'

launchctl setenv STATHAT_EZKEY $STATHAT_EZKEY
export ET_SCRIPT_PATH=$HOME/EverTrue/dev/core/script
export PATH=/usr/lib/php/pear:/usr/local/bin:$PATH:$HOME/.rvm/bin:$HOME/EverTrue/devops/evertools/prebuilt/evertools-1.0-SNAPSHOT/bin:/usr/local/share/npm/bin # Add RVM to PATH for scripting
